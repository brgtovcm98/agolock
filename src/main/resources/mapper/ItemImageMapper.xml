<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seu.seustock.mapper.ItemImageMapper">

    <update id="unsetPrimaryByItemId" parameterType="long">
        UPDATE item_images
        SET is_primary = FALSE
        WHERE item_id = #{itemId}
    </update>

    <select id="countByItemIdAndImageId" resultType="int">
        SELECT COUNT(*)
        FROM item_images
        WHERE item_id = #{itemId}
          AND image_id = #{imageId}
    </select>

    <update id="updateItemImage">
        UPDATE item_images
        SET display_order = #{displayOrder},
            is_primary = #{primary}
        WHERE item_id = #{itemId}
          AND image_id = #{imageId}
    </update>

    <insert id="insertItemImage">
        INSERT INTO item_images (item_id, image_id, display_order, is_primary)
        VALUES (#{itemId}, #{imageId}, #{displayOrder}, #{primary})
    </insert>

</mapper>
